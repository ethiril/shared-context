welcome-email-job@1.0.0 [active] @2026-01-10T09:00:00Z by api: v1 job schema — userId, email, locale. Enqueued on signup by API; consumed by worker. | consumers: worker | breaking: false | queue: name=welcome-emails backend=Redis-bull vt=30s | payload: userId(string;req;UUIDv4 or ULID); email(string;req;RFC 5322); locale(string;req;BCP-47, v1 worker treats any value as en) | producer: API enqueues exactly once per successful signup; no dedup. | consumer: Worker idempotent if retry added v2; not required for v1. | coupling: fire-and-forget
